import React from "react";

const Header = () => {
  return (
    <header
      style={{
        padding: "1rem",
        textAlign: "center",
        backgroundColor: "#f1f1f1",
        color: "blue",
      }}
    >
      <h1>React Modularity Assignment</h1>
    </header>
  );
};

export default Header;
